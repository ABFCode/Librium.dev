# Changelog

## 0.17.0 - 2026-07-31
- **Cross-device sync overhaul: a stuck device recovers by itself — no more reload-to-sync.** The reported bug: a phone that read ahead could sit on its chapter indefinitely while the PC showed an old position, until a full page refresh shook it loose. Root cause: every sync hook (progress, status, bookmarks, collections, settings) serialized its server pushes on an unbounded promise chain — one mutation whose acknowledgement never arrived (frozen mobile tab, half-dead websocket) blocked the chain forever, and every later wake queued behind it. All five hooks now share a single `SyncPushQueue` (via `useSyncPushQueue`): passes still serialize, but a push wedged longer than 15s of awake-online time is abandoned and retried — detected both lazily (every edit/wake) and by an armed stall timer, so even a tab nobody touches recovers on its own. Reconnects and tab-resume events grant a fresh grace period (a mutation legitimately held while offline or suspended is never duplicated), abandoned passes learn they're superseded via the heartbeat and stop instead of replaying stale rows, and the live cross-device hand-off then does what it always intended: switch chapters on the phone, the PC follows in real time.
- **Device-aware conflict resolution (server).** Abandoning a wedged push created a hard question the old code answered wrongly in both directions: was that rejection another device's newer write, or the echo of *my own* write whose acknowledgement got lost? Three independent reviews converged on the same verdict — no client-side heuristic answers this safely (a first attempt shipped in this branch and was torn back out after its review). The real answer lives server-side in one rule (`syncVersion.rejectsStaleBase`): a stale-base write is only a conflict if the current server value was authored by a *different* device — your own unacknowledged write is causally yours, so the server accepts the re-push. All five versioned mutations now carry it (`updateProgress`, `updateStatus`, per-field `userSettings.upsert`, collection rename, membership add/remove) with new optional writer-device schema fields (`statusDeviceId`, `fieldDeviceIds`, `nameDeviceId`, `writerDeviceId`; `progressDeviceId` already existed). Stateless across reloads, remounts, and tabs; genuine cross-device conflicts still resolve server-wins; clients that send no `deviceId` keep the old strict rejection unchanged.
- **Settings sync correctness.** Field settlement no longer trusts value equality (an old acknowledgement for "night" could silently absorb a newer re-edit back to "night", stranding it locally forever): `LocalReaderSettings` gains per-field edit stamps (`fieldEditedAt`) and only the response for a field's newest edit may settle it. Synced versions are now strictly monotone (a late response can't wind them backward), and an accepted-but-normalized value (server clamps ranges and whitelists themes) is adopted instead of misread as a rejection — which under the retired hazard design would have re-pushed the raw value forever.
- **Tested like it matters.** The sync plane got the adversarial treatment: three reviews (two independent agents plus an 8-angle finder/verifier pass) produced 10 verified findings, 9 fixed and 1 accepted as a documented tradeoff; every fix is pinned by a test that provably fails when its guard is sabotaged (device rule, stall timer, resume grace, supersession — all mutation-tested). 260 tests across the three vitest projects, including new convex suites driving the real mutations through same-device/foreign-device/legacy trios and a randomized chaos test that models Convex faithfully — FIFO apply-on-arrival with responses delayed or *lost* (the applied-but-unacknowledged case), plus a concurrent foreign writer — asserting convergence. Live end-to-end: 17 Playwright tests against a real local Convex backend, including a new `liveFollowRecovery` spec that walks the originally-reported scenario (chapter change follows live across two devices; an offline device's read-ahead hands off after reconnect with no reload). Writing that spec honestly also caught a vacuous assertion in the existing `progressHandoff` e2e — `/Chapter II/` substring-matches "Chapter III", so its final hand-off checks could never fail; both specs now use anchored matchers.
- **Deploy note:** the Convex functions must deploy **before** the web client (`npx convex deploy`, then merge). The new client sends a `deviceId` argument the old backend's validators would reject; the new backend is fully backward-compatible with old clients.

## 0.16.0 - 2026-07-10
- **@abfcode/spine 0.9 → 0.11.** Two upstream releases in one bump, zero adaptation needed (all 140 tests and the real-book corpus pass unchanged). 0.10 is spine's pre-1.0 hardening release: DEFLATE-bomb ceilings on zip inflation (a hostile EPUB degrades instead of exhausting memory), ruby annotations handled correctly for CJK books (furigana readings dropped from extracted text, so search sees 漢字 not 漢かん字じ), and the .txt chapter splitter's weak rules gained consistency bounds — detected-break precision went from 6.9% to ~52% against a ground-truth oracle. 0.11's data-model pass makes the writer's simple metadata fields authoritative and teaches the cover to own its path (both of which our 0.13-era export workarounds already anticipated — they keep working and are now belt-and-suspenders), and fixes title/series extraction for books that list subtitles or non-series collections first. `PARSER_VERSION` tracks spine, so every library re-parses on next open and picks all of this up retroactively.
- **Password reset, at last.** "Forgot password?" on the sign-in page emails a reset link (new `/forgot-password` and `/reset-password` pages, anti-enumeration confirmation copy, expired-link recovery). Mail goes out as `hello@librium.dev` through Resend via the official `@convex-dev/resend` Convex component (durable queue, exactly-once), wired into Better Auth's `sendResetPassword` — plus email verification plumbing behind `REQUIRE_EMAIL_VERIFICATION` for when public signups open. Everything is env-gated on `RESEND_API_KEY` (absent = skipped with a logged warning, auth flows unaffected), and the reset endpoint gets its own rate limit (5/min) so it can't flood a victim's inbox. Live-verified end to end: request → emailed token → new password works, old refused.
- **Account deletion is one command.** `npx convex run admin:deleteUserAccount '{"email":"...","confirm":"DELETE"}' --prod` fulfills the privacy-page promise as a single operator-run action: every app row across all seven tables, the R2 objects (derived keys, so mid-import books can't strand blobs), and the Better Auth records (sessions, credentials, verifications, user) — with accounts that signed up but never imported handled too. The Polar customer is deliberately left to Polar (merchant-of-record retention obligations). Cross-contamination pinned by tests (a second user's rows survive untouched) and smoke-tested live: sign up → import → delete → the same email can immediately register fresh.

## 0.15.1 - 2026-07-09
- **Menus stopped being finicky.** Every popover menu (library ⋯, collection filter, card menus, Mark as…) dismissed on hover-out — drift a pixel past the edge and it closed. All of them now dismiss the way native menus do: click outside or Escape (`useDismissable`, one shared hook — also retiring the review's deferred dialog-dismiss item for menus).
- **Account & storage is findable and explains itself.** A person icon in the library toolbar opens it directly (the ⋯ entry stays). The dialog now shows your usage against your plan's actual allowance (with a "not enforced yet" note while that's true), side-by-side Free/Supporter plan cards with each plan's storage, and the real price on the checkout button — read live from Polar's synced catalog, so the UI can never advertise a number checkout doesn't charge. Backing that: `getStorage` reports both plans' allowances regardless of enforcement and `getConfig` includes the product's price.

## 0.15.0 - 2026-07-09
- **Supporter subscriptions + cloud-storage quota (Polar).** Librium can now pay for itself: the free plan gets a cloud-storage allowance (default 250 MB of stored EPUBs — local reading is never limited), and a yearly Supporter subscription raises it (default 10 GB). Payments run through Polar as **merchant of record** — Polar is the seller and handles checkout, receipts, global VAT/sales tax, and refunds; Librium stores only the subscription status (synced by webhook into the Convex Polar component). New "Account & storage" dialog (library ⋯ menu) with a usage bar and checkout/customer-portal links; the import page shows usage while enforcement is on. **Data-safety rules, in priority order:** quota gates *new uploads only* — sync, seeding, reading, export, metadata edits, and deletes are never quota-checked, and a lapsed subscription locks nothing, ever. **Enforcement is on verified bytes, not client claims:** a new `finalizeUpload` action HEADs R2 for the object's true size and attaches inside a serializable mutation (concurrent imports can't race past the limit together); rejected uploads are deleted from the bucket and the local copy is untouched; `fileSize` now always records what R2 actually stores. Covers live outside the quota behind a 10 MB sanity cap. Everything is env-gated (`QUOTA_ENFORCED`, `POLAR_*`) and defaults to off/free, so the code deploys before the Polar org exists. Billing lookups fail open to the free plan — a Polar outage can never block reads or grant paid limits. New `/terms` and `/privacy` pages: plain-language, with an explicit wind-down clause (3 months' read-only notice + refunds + export always works) and a complete data inventory.
- **Review hardening (billing):** the 8-angle pre-release review confirmed 18 findings, 15 fixed before anything ships. The load-bearing ones: a finalize retry on an already-attached book could hit the quota-rejection path and **delete the book's live cloud EPUB** (now short-circuits to success; epub upload URLs also can't be re-minted once attached, making the master immutable); the quota was bypassable by uploading and simply never finalizing (public `api.r2.generateUploadUrl` removed, `deleteBook` deletes derived keys unconditionally, and a **daily orphan sweep** reclaims unclaimed `books/*` objects — accounting now converges to bucket reality); a Polar product created before the webhook existed made `getCurrentSubscription` throw, so a **paying supporter would have silently resolved to the free plan** (`billing:syncProducts` + loud fail-open logging); an oversized replacement cover destroyed the existing cover before validation (client refuses before the destructive fixed-key PUT); a quota-rejected import stranded a phantom metadata-only book on every device (now cleaned up); and a cover failure after the EPUB attached marked the import "failed", inviting duplicate re-imports (now a warning on the done item). Deferred knowingly: per-check usage summation (a denormalized counter risks drift in exactly the numbers that must stay honest), shared dialog-dismiss hook.
- **Adversarial + property tests for the quota plane** (`quota.convex.test.ts`, in CI): a fast-check property drives arbitrary interleavings of registers/attaches/deletes against the real Convex functions asserting the core invariant — verified attached bytes never exceed the limit, rejections change nothing, deletes free space — plus targeted adversarial cases: a client lying about file size is caught at finalize, non-finite/negative sizes rejected, re-finalize doesn't double-count, cross-user finalize refused, the legacy attach path refuses while enforcement is on (no unverified bypass), oversized covers rejected with the dangling key cleared, quota strictly per-user, and over-limit users keep full read/delete access. Proven falsifiable (sabotaged double-count and weakened limit both caught).

## 0.14.0 - 2026-07-09
- **Import .txt and .md webnovel rips.** Drop a raw text rip on the import page and it becomes a first-class book: spine 0.9.0's text ingestion (`@abfcode/spine/text`) converts it to a real EPUB — chapter detection via an ordered heuristic ruleset (Chapter N, roman numerals, CJK `第N章`, markdown headings, ALL-CAPS runs; biased to under-split), Gutenberg banner stripping (guarded), title from an authored markdown H1 or the filename — and then it rides the *entire* existing pipeline unchanged: R2 gets a proper EPUB, so seeding, sync, metadata editing, and export-with-write-back all work identically. Conversion runs in a module worker with the same in-page fallback shape as parsing. Verified live: the raw Project Gutenberg Alice `.txt` imports as a 29-chapter book. Bumping spine 0.8→0.9 also re-parses the library on next open (`PARSER_VERSION` tracks spine), though parser behavior is unchanged in 0.9.
- **Hardening pass.** A cleanup + testing + review sweep with no user-facing change. **Property-based sync tests** (fast-check over the real merge functions via convex-test, wired into CI) now guard the two documented bug classes across *arbitrary* operation orderings — the multi-device reality: status LWW always resolves to the highest-editedAt write regardless of apply order, and bookmark tombstones never resurrect (a key ever deleted stays tombstoned, same-key creates never duplicate). Both proven falsifiable. **Full-repo security review** came back clean: every mutation/query/action enforces ownership (read-URL signers, R2 key-prefix binding, collection/bookmark id-ops, the delete path), the metadata actions keep their SSRF guards (https-only, private-host block, per-hop redirect revalidation, size caps), admin/seed functions are env-gated off in production, no `dangerouslySetInnerHTML` anywhere, secrets are env-only, and the extension holds minimal permissions; `pnpm audit` clean. **Refactor:** the 1,969-line `ReaderExperience` shed its parsed-block renderer (`ReaderBlocks`) and link-resolution helpers (`readerLinks`) — behavior-identical, down to 1,616 lines. Plus the intermittent `library.spec` CI flake fixed at its root (an async-merge poll raced the ~1s default), `pwa.ts` split into its two concerns, shared test fixtures, and the codebase's last stale TODO retired.
- **Immersive reading on mobile.** The top bar no longer stays pinned — on phones it slides away as you read and returns when you scroll to the top or tap the page, so the text owns the screen. A tap anywhere (outside links/images/selection) toggles the chrome: the top bar plus a new **thumb-zone bottom bar** — ‹ Prev · chapter/progress · Next › where the center opens the chapter sheet. No tap-to-advance and no edge zones, so there are no accidental jumps; you scroll to read and tap to reveal navigation. The chapter sheet itself rises as a **bottom sheet** on phones (full-width, rounded top), auto-scrolled to your current chapter. Toggle decided on `click` not pointerup, so the tap's own synthetic click can't hit freshly-rendered chrome and undo itself.
- **Persistent-storage request moved to first download.** The browser's "keep this stored?" prompt (Safari eviction mitigation) now fires when a book's content first lands on the device — import or download-to-device — instead of at login, so the ask is self-explanatory. Denying it costs nothing functional: an evicted library just re-seeds from R2 on next open.
- **Review hardening (txt import):** the pre-release review confirmed and fixed 4 findings — `fileSize` now records the converted EPUB actually stored in R2 (not the source rip's size; `fileName` deliberately keeps the source name as provenance), spine's `stripBoilerplate` data-loss warnings surface to the console instead of firing into the void, the import page's empty-queue error stopped saying "EPUB file", and the accepted-extensions filter composes `isTextImport` instead of a third hand-rolled regex. Deferred knowingly: a shared worker-with-fallback helper (rule of three met, but unifying three proven worker paths is its own refactor) and the server-side title-fallback regex (unreachable — spine floors titles at "Untitled").
- **Book downloads got a real progress bar.** The reader's first-open seeding state ("Downloading book to this device…" as bare text) now streams the R2 download and shows an actual percentage — paragraph shimmer, a thin gold bar, "Downloading book — 42%", then "Preparing book…" while the parse runs. The heading shows the book's title instead of "Untitled chapter" while no section exists yet. Verified live under a throttled connection.

## 0.13.0 - 2026-07-08
- **Mobile ergonomics:** chapter navigation now lives where the reading stops — a chapter-turn block at the end of every chapter (muted Previous, ink-weight Next with the next chapter's title, "The end — back to library" on the last), sized for thumbs and scaled to the reading font. Icon buttons grow to Apple's 44px floor on touch devices (were 34px). The book-card menu no longer hover-opens *and* click-toggles — a race where the pointer's own hover opened the menu and the click closed it again, which broke every touch tap (synthetic mouseenter precedes click) and was the source of the intermittent `library.spec` CI failure.
- **Loading states grew shapes:** cover-grid shimmer on the library shelf, row shimmer in the chapter list, paragraph shimmer while a chapter's blocks arrive — replacing bare "Loading..." text. Meaningful copy (downloading to device, restoring position) stays as words.
- **Dark-mode images, actually fixed:** dark reader themes print images on a warm paper backing — Standard Ebooks-style engravings are black ink on *transparency* and simply vanished on dark pages; opaque white scans keep the glare dimming from 0.12.0's follow-up. Verified against Tenniel's plates in the illustrated Alice.
- **PWA polish (deferred-list item, closed):** persistent storage requested once signed in (Safari evicts IndexedDB after ~7 idle days otherwise — the silent-library-loss failure mode), an Install entry in the library's ⋯ menu (real prompt on Chromium, Share → Add to Home Screen hint on iOS), and a real icon set (PNG 192/512/maskable + opaque apple-touch-icon; Android install criteria reject SVG-only manifests, iOS ignores manifest icons entirely).
- **Export bakes your metadata in (deferred-list item, closed):** exported EPUBs are rebuilt via spine's writer with the edited title/author/series/description and replaced cover — a locally edited title and author leave the app with those edits embedded. Best-effort in a worker with graceful fallback to the raw R2 copy; round-trip tested including a large illustrated public-domain book. Overrides also replace spine's refined metadata layers (titles[], descriptions[], belongs-to-collection), which otherwise shadow the simple fields.
- **Review hardening:** an 8-angle review of this batch confirmed and fixed 8 findings before release. The big one: replacing a cover left the source cover in the writer spec, so re-exporting any previously-exported file collided with the writer's default cover path (`duplicate_href`) and silently fell back to the raw copy — reproduced live, fixed by dropping the stale resource, regression-tested with a double round-trip. Also fixed: cleared fields (series/description/author) now clear in the export instead of resurrecting from the source (the local row is the identity — absent means cleared); the 44px touch buttons no longer overlap card titles (padding raised in step) or the reader drawer (anchor lowered on coarse pointers); the PWA icons joined the service-worker precache (workbox glob omitted png); the export worker gained the same construction-failure fallback its parse sibling has; the tombstone sweep got real `by_deleted` indexes instead of an unindexed scan that could hit Convex's read ceiling at scale; and the two identity-field lists (re-seed vs export) now cross-reference each other. Deferred knowingly: per-book worker spawns in bulk export, small test-constant duplication.
- **Tombstone compaction (the codebase's one TODO, closed):** a daily Convex cron deletes bookmark/collection tombstones older than 90 days, per the sync design's fixed-horizon rule; README's feature list caught up with reality (it described ~v0.5).

## 0.12.0 - 2026-07-07
- **@abfcode/spine 0.1.1 → 0.8.0.** Seven minor versions of the parser in one jump, adapted end to end: the reader's inline renderer moved to the 0.7.0 model (styling is combinable `emph`/`strong`/`code` flags, so `<strong><em>x</em></strong>` no longer loses a style; legacy-kind blocks still render until they re-parse), the ingest pipeline stopped re-resolving image srcs and link hrefs (spine ≥0.6 pre-resolves them to archive-relative paths — re-joining corrupted every image path, caught by a new real-EPUB corpus test), internal link resolution tries the pre-resolved href directly before falling back to relative resolution for old blocks, and search text extraction no longer injects a space around every styled run (`(sic)`, not `( sic )`). `PARSER_VERSION` now comes from spine's own `VERSION` export, so this bump — and every future one — automatically re-parses the whole library from raw EPUBs on next open, picking up 0.2–0.8's fidelity fixes (nested styling, malformed-XML recovery, entity handling, calibre series fallback) retroactively. Parsing is 5–7× faster upstream; the full 6MB/1,003-section memorize testbook parses in well under a second.
- **EPUB parsing moved off the main thread.** spine 0.4.0 dropped its DOMParser dependency, unblocking the roadmap's deferred Web Worker item: imports and device seeding/re-parse now run in a module worker (image/cover bytes transfer back zero-copy), so a 2,000-chapter webnovel import no longer freezes the UI. Environment failures degrade to a main-thread parse — the worker is an optimization, never a point of failure — and the degraded path logs a marker that the reading e2e asserts is *absent*, proving the worker really runs in a real browser (vitest's browser mode can't host Vite module workers, so unit tests cover the fallback and e2e covers the worker). The worker chunk rides the PWA precache, so offline seeding keeps working. Verifying this also caught a real bug: a parse failure inside the fallback path previously hung the import promise forever.
- **EPUB corpus tests** (`epubCorpus.test.ts`), two tiers: a synthetic image-bearing EPUB always runs (CI included) and pins the src-resolution invariant — every block-referenced image must resolve to extracted bytes (the regression that would have shipped without it); the real `testbooks/` novels additionally run on dev machines (structure pinned: 1,003 sections for memorize, 32 for worm4) but are copyrighted files kept out of the public repo, so those skip in CI.

## 0.11.0 - 2026-07-07
- **NovelUpdates clipper extension** (`extension/`): a one-click companion for the ~100x webnovel-linking flow — right-click → "Copy to Librium" (or the toolbar icon) on any NU series page copies the page HTML *and* the cover image as one JSON clipboard payload; pasting into the Edit-details dialog fills the source URL, opens the candidate diff, and stages the cover in one go. Getting the cover took three attempts, live-tested against real Cloudflare: extension background fetch is 403'd even with `credentials:"include"` (the clearance cookie is partition-bound to the tab), page-context fetch is CORS-sealed (CDN sends no `Access-Control-Allow-Origin`), so the shipped path **screenshots the rendered page and crops the cover** (`captureVisibleTab` + `OffscreenCanvas`) — no network request, nothing for Cloudflare or CORS to judge. That API demands the `activeTab` grant, which browsers hand out only for clicks on extension UI — hence context-menu/toolbar triggers and no injected button (a v0.1.x floating button could never capture; replaced by a status toast). Both doomed fetch paths were deleted after the live verdicts, so a successful run's console stays clean. Cross-browser MV3 (Chrome service worker + Firefox event page via dual background keys; `browser_specific_settings.gecko` set), host access scoped to novelupdates.com only, no server contact — clipboard out only. Versioned payload contract (`librium: 1`, diagnostic `coverError` on failure) parsed by `parseLibriumPayload` with per-field validation; unrecognized pastes fall through to normal text behavior. `parseNovelUpdatesHtml` is verified against a minimal, entirely synthetic selector-contract fixture.
- **Covers past Cloudflare:** NovelUpdates fronts its image CDN with the same Cloudflare wall as its pages (verified: `cdn.novelupdates.com` 403s every server-side fetch regardless of User-Agent), so the cover proxy could never fetch an NU candidate's cover and Save died with a bare 403 *after* the text fields had already committed. Three fixes: fetched-cover downloads now try the **user's browser first** (real browser traffic passes Cloudflare, and CORS-friendly hosts like Open Library / Google Books allow reading the bytes) with the server proxy as fallback; a cover failure is **non-fatal** — details stay saved and the dialog explains the way forward instead of a bare error; and you can now **paste an image (Ctrl+V)** anywhere in the Edit-details dialog to set the cover — right-click → Copy image on the NU page, paste, Save. No download-to-disk required.
- **Review hardening (clipper + cover pipeline):** an 8-angle review of the extension and dialog changes confirmed and fixed 6 findings. Highlights: applying an NU candidate's diff no longer clobbers a staged file cover (pasted/extension-captured) with the candidate's url cover — the cover row is checked by default and NU CDN urls are guaranteed to 403 at Save, so `applyFetched` now never downgrades file→url (falsifiable regression test included); the browser-first cover download enforces the same 4MB cap as the server proxy (an oversized direct hit previously uploaded uncapped); the extension's context menu survives Firefox restarts (removeAll+create on `onStartup`, not just `onInstalled`); a clipboard image item that yields no File no longer swallows a co-present payload paste; a truncated clipper payload is caught by prefix instead of dumping ~200KB of JSON into the focused field; and a payload whose cover fails to decode now says so instead of silently proceeding. Cleanups: the extension's two dead fetch paths (~120 lines, unreachable since every real NU cover lives on the blocked CDN) are deleted along with the now-unneeded `cdn.novelupdates.com` host permission, and the dialog's three copy-pasted pending-cover constructions collapsed into one `setCoverFromBlob`.
- **"Recent" sort no longer moves when you set a status.** Marking a book (or bulk-marking) bumps the userBooks sync clock — which the cross-device status merge legitimately needs — but that same clock also drove the default Recent shelf, so shelving an old book as Abandoned vaulted it to the top past what you were actually reading. Reading recency now lives in its own `lastActivityAt` field, written only by opening/reading a book (and at import), so status edits leave the shelf order alone. Never-opened books marked from the shelf sort last in Recent, as they should. New falsifiable e2e (`recentSort.spec.ts`) that fails against the old behavior — writing it also exposed that its first draft passed vacuously by racing the async status push, now synchronized.

## 0.10.0 - 2026-07-07
- **Edit details:** every book gets an editor (card menu → "Edit details…") — title, author, series/index, description, language, and a source-page URL; replace the cover from a local image. Saves only changed fields (empty = clear); server-authoritative and online-only, mirrored into the local shelf row so offline views agree. Replaced covers propagate to other devices via a `coverUpdatedAt` stamp (the R2 key never changes, so staleness is version-checked, on the server's clock).
- **Metadata fetch:** a "Fetch metadata" panel searches Open Library (no key) and Google Books (set `GOOGLE_BOOKS_API_KEY` on the Convex deployment to enable) — ISBN-first when the EPUB carried one, title/author otherwise. Candidates show covers and sources; picking one opens a per-field diff (current vs proposed, checkboxes) that merges into the form — **nothing touches the server until Save**. Fetched covers download through a server proxy (image hosts don't send CORS) with https-only/private-host/size guards, then ride the normal R2 + local-blob path.
- **NovelUpdates linking:** paste a series URL into the source-page field — "Open source page" everywhere it's shown, and "Fetch from linked page" pulls title/author/description/genres/cover from that exact page (no search matching). NU's Cloudflare blocks server fetches routinely, so the flow degrades to paste-the-page-HTML, parsed client-side into the same diff preview. The page-fetch action is allowlisted to novelupdates.com only.
- **Re-seed identity preservation:** re-downloading a book's content (device seeding, stale-parser refresh) no longer resurrects the EPUB's embedded metadata or cover over user edits — re-parse refreshes content, never identity. Covered by e2e (edit → remove download → re-download → edit intact).
- Reconcile now also mirrors drifted server metadata into local rows and invalidates stale local cover blobs; the R2 upload helper is shared (`uploadBookAsset`) between import and the editor.
- **Review hardening:** a 6-angle adversarial review (both 0.9.0 and 0.10.0 code) confirmed and fixed 18 findings across sync, security, and UI. Highlights: a collection membership un-checked during its create/add round-trip is no longer stranded alive on the server (dirty-flag stomp past a tombstone); status/collection merge writes now re-read the live row in a transaction so a concurrent edit can't be clobbered back to `dirty:0`; the metadata fetch actions re-validate **every redirect hop** (SSRF-via-redirect close), block IPv6 loopback/ULA/link-local/mapped hosts, and stream-abort oversized bodies instead of trusting `Content-Length`; `attachFiles` binds the R2 key to the book's own prefix (closes a cross-user object-read via a forged key); the Edit-details dialog diffs against its open-time snapshot (not the live doc) so a concurrent edit from another device isn't reverted on Save; "Select all" selects only the visible/filtered shelf; the delete-collection confirm no longer renders under the Manage dialog; a stale reconcile run can no longer demote a freshly-seeded book to content-absent (cancellation guard + transactional insert); the import path stamps `coverVersion` so a freshly imported cover isn't dropped and re-downloaded; and Escape no longer both closes a dialog and drops the multi-selection. Low-severity sync edge cases (LWW loser convergence, offline account-switch) and UX papercuts (paste-to-parse, search using live form terms) also fixed. A dedicated data-integrity pass over the reconcile/seed/cover machinery found no persistent-corruption defect. Two known-lows are deferred: marking a status bumps the "Recent" sort (needs a separate recency field), and cover *replacement* isn't atomic across the R2 upload + `attachFiles` stamp (an interrupted replace can show a stale cover on other devices until a retry — inherent to the fixed cover key).

## 0.9.0 - 2026-07-07
- **Reading status:** every book has a status — Reading / Want to read / Finished / Abandoned — set from the card menu (re-picking clears back to Automatic) or in bulk via "Mark as" over a selection. Unset books derive their status from progress (untouched → Want to read, started → Reading, ≥99% → Finished). Local-first with LWW sync on its own clock (`statusEditedAt`, disjoint from progress), so status edits work offline and never clobber or get clobbered by progress writes.
- **Collections:** user-named, many-to-many book groups (a book can live in several) — "Add to collection…" from the card menu or over a bulk selection opens a picker with inline create; a Manage dialog renames/deletes. Fully offline-capable on the bookmark tombstone pattern (idempotent clientKey creates, deletedAt tombstones), with one new rule: memberships reference collections by client key, and pushes send collections first so books added to an offline-created collection sync correctly on reconnect.
- **Series shelves:** a new "Series" sort groups the library by the EPUB's series metadata, volumes ordered numeric-aware ("Vol 2" before "Vol 10", fractional indexes like "1.5" in place); standalone books trail in one group. Works offline (series fields mirrored into the local shelf rows).
- **Shelf filters:** a filter row under the header — status tabs, an "On this device" toggle (downloaded-only), and a Collection dropdown — all composable with search and sort, persisted across visits (`library:filters`).
- **Sync hardening:** push passes now serialize on a promise queue and re-read dirty rows from IndexedDB — a change landing while a push was in flight could previously be dropped, stranding unsynced edits until the next unrelated write.
- **Library toolbar extracted** (`LibraryToolbar`) — LibraryView had grown past 1,100 lines; the header/filter chrome is now a presentational component.
- New e2e journey (`library.spec.ts`): status via menu → shelf tabs → collection create/file/filter → reload persistence; browser tests for the collection-sync push ordering, picker dialog, and every new filter.

## 0.8.1 - 2026-07-06
- **Adopt Biome:** replaces the absent lint/format setup with Biome 2.5 (recommended rules + type-aware promise-safety rules) as a single dependency; `biome ci` gates CI ahead of the typecheck. One-time reformat + lint fixes across 76 files — missing button `type` attributes, non-null assertions replaced with real narrowing (surfaced one latent type hole in `authHelpers.ts`), module-scoped pure helpers pulled out of `ReaderExperience`, a11y suppressions documented where pointer-only affordances (backdrop click-to-close, hover-out menus) are intentional.

## 0.8.0 - 2026-07-06
- **UX refresh (design system):** one accent color (gold; red is destructive-only, muted green is semantic success), flat surfaces on a single radius/shadow scale, no pill shapes or tracked-uppercase micro-labels, calmer background. Every screen reflowed: library gets a page heading + quiet stats line and a denser cover grid; import is a single centered drop zone with the queue appearing beneath; auth/landing are single centered cards with the marketing filler removed.
- **Reader rework:** the reader owns the full viewport (app header hidden on `/reader`) — slim top bar with back-to-library, chapter title, and position ("N / M · X%"); full-height text column (no more 70vh box). Chapters/search/bookmarks live in a compact popover under the ☰ button (book title + "Chapter N of M" header, auto-scrolls to the current chapter, closes on selection/Esc/click-outside). Kindle-style page-turn zones in the margins (hover chevrons, wheel-transparent, deltaMode-aware for Firefox).
- **Whole-book search:** the drawer's Search tab scans the entire book from IndexedDB — built for 2,000-chapter novels (per-book pre-lowercased text cache built once with event-loop yields, debounced cancellable scans, capped results). Results show their chapter and jump across sections to the exact paragraph.
- **Scroll position rework:** positions (progress *and* bookmarks) are saved as layout-independent anchors — block index + fraction within it — so they restore exactly across devices, font sizes, and widths. Restore is instant with content visible (no more blank-out/"Restoring position…" overlay); fonts/images settling and font-size changes silently re-anchor; scroll saves settle with a trailing emit. Not backward compatible with old pixel offsets (WIP data).
- **Progress percent, correctly:** completed chapters plus the fraction of the current one (new `sectionFraction` synced end-to-end) — fresh books read 0%, 1-chapter books progress, finishing a book reaches 100%.
- **Library multi-select:** Select mode with per-cover check badges; bulk Download / Export EPUBs / Remove downloads / Delete over the selection, plus Select all. Bulk operations share one list-parameterized implementation with the whole-library actions (now in an overflow ⋯ menu).
- **Export:** "Export all EPUBs" (and per-selection export) pulls the master copies from R2 (egress-free), fetched to blobs so files are named `<Title>.epub` instead of the R2 key basename.
- **In-app dialogs:** a styled ConfirmDialog replaces every native `window.confirm`/`prompt` in the library — Esc/click-outside cancel, destructive flows use an inline type-DELETE input; no global Enter-to-confirm (Enter on Cancel used to execute the action).
- **Settings:** font size is a 12–36px slider with a type-in px field; reading font choice (Sans/Serif — IBM Plex/Fraunces, both self-hosted); modal restyled with live preview behind a light backdrop, Esc/click-outside close.
- **Parser fix ships (@abfcode/spine 0.1.1):** inline text runs keep their boundary spaces ("what *are* you", not "whatareyou"); `PARSER_VERSION` bump re-parses stored books from the raw EPUB on next open, healing the whole library retroactively.
- **Storage hygiene:** the library storage figure reads the IndexedDB portion, samples after deletes settle, displays whole MBs, and an orphan sweep (once per visit) clears content rows left by interrupted deletes and legacy dev data.
- **Review hardening:** a branch-wide 8-angle review confirmed and fixed 19 findings — highlights: cross-section jumps no longer anchor against the previous chapter's DOM, stale re-anchor closures are token-invalidated, Escape layering respects open dialogs, `fontFamily` can't be reset by stale clients, and the search cache invalidates on re-parse.

## 0.7.0 - 2026-07-05
- **Deployed (ROADMAP Phase 7 — complete):** Librium now runs at a real URL, **[librium.dev](https://librium.dev)**, on the target zero-ops stack — Cloudflare Pages (static app, auto-deploy on push to `main`) + Convex Cloud (auth + sync) + Cloudflare R2 (raw EPUBs/covers), with a dedicated prod R2 bucket and CORS. Infra cost is $0/mo at current library size.
- **Closed registration:** sign-up is disabled by default on deployed instances (`emailAndPassword.disableSignUp`); reopen at release by setting `ALLOW_SIGNUP=true` on the Convex deployment (env-only, no redeploy). Admin-reset and seed functions are internal-only, so browser clients cannot invoke them on any deployment. Sign-in is unaffected. The UI reflects the state via a public `config.signupEnabled` query: the sign-up page shows a "Registration is closed" panel and the Sign up / Create account links are hidden.
- **Auth rate limiting:** auth endpoints are throttled on deployed instances via Better Auth's rate limiter with `storage: "database"` (counters persist in the component's `rateLimit` table — serverless-safe), sign-in capped tighter (20/60s) than the 100/60s global default. Disabled locally so dev/tests aren't throttled.

## 0.6.0 - 2026-07-05
- **Import queue UI fix:** long titles truncate instead of blowing out the layout.
- **Bulk library operations:** toolbar actions — "Download all" (pre-load every book onto this device), "Clear downloads" (confirm; frees local storage, library untouched), and "Delete all" (type-DELETE confirmation; removes every book + cloud backup everywhere), each with progress feedback.
- **Bulk import (ROADMAP Phase 6):** the import page is now a queue — drop many EPUBs or an entire folder (folder picker + recursive folder drag-drop), each file imports sequentially with per-file status (Queued/Importing/Ready/Failed) and failures skip instead of aborting the batch.
- **Per-device download management (ROADMAP Phase 6):** the library shows which books' content is cached on this device (dot on the cover + "N/M on device · storage used" summary). Book menu: "Download to this device" (pre-load without opening), "Remove download" (free local space; the book, its progress, and bookmarks are untouched everywhere), "Save EPUB" (renamed from Download), "Delete book" (renamed from Remove).

## 0.5.0 - 2026-07-05
- **Structured R2 keys:** objects live under `books/{bookId}/…` (book.epub + cover) via an ownership-checked upload-URL mutation — the bucket is self-documenting.
- **Blobs → Cloudflare R2 (ROADMAP Phase 5):** raw EPUBs + covers now live in R2 (10 GB free, zero egress) via `@convex-dev/r2`; Convex storage holds no blobs. Parsed content is treated as derived data: new devices seed a book by downloading the EPUB from R2 and re-parsing it locally (`parserVersion` is stamped per book — a device with a stale parse re-seeds automatically, so parser improvements apply retroactively).
- **Backend demolition:** with content derived client-side, the `sections`, `bookAssets`, `bookFiles`, and `importJobs` tables and their modules (`ingest`, `sections`, `reader`, `bookAssets`, `bookFiles`, `importJobs`, `storage`) are gone. Import is now: parse in browser → one `registerImport` mutation → readable immediately from IndexedDB → EPUB/cover upload to R2 in the background (`attachFiles`). No batched section ingest, no Convex-id backfill; progress and bookmarks reference section indexes only. `deleteBook` no longer needs chunked deletes and also removes the R2 objects.
- **Breaking:** schema changed; dev data was reset (re-import books). Seeding now creates the demo user only (`make convex-seed`).

## 0.4.0 - 2026-07-04
- **Deleted-book handling:** a book deleted from another device no longer error-pages a reader that has it open — the reader purges the local copy and returns to the library (`books.getBook` null signal; reader-path queries return empty instead of throwing).
- **Local-first library + bookmarks (ROADMAP Phase 4, complete):** the library shelf renders from IndexedDB when offline (covers cached as local blobs; progress badges derived from local records) and reconciles against the authoritative server list when online — books deleted on another device are purged locally, and books imported elsewhere appear on every device's shelf. Bookmarks are local-first with tombstone sync: create/delete work offline, offline creates push idempotently on reconnect (client keys), and deletes propagate as server tombstones instead of being resurrected by other devices.
- **Local-first progress + LWW sync (ROADMAP Phase 4, slice 1):** reading progress is written to IndexedDB first (instant, offline-durable) and synced to Convex with last-write-wins: dirty local edits push on edit and on reconnect; the server rejects pushes older than what it holds (`progressEditedAt`), so a stale queued write can never clobber newer progress; pulls adopt remote state only when its server timestamp is newer than the last merged state and never over unpushed local edits. The reader restores from the merged view — offline reopen resumes at the exact local position; a newer remote position corrects the view only within moments of opening and never after the reader has navigated (no mid-session yanks).
- **PWA shell (ROADMAP Phase 3):** `vite-plugin-pwa` service worker precaches the app shell (JS/CSS/fonts/icon) with SPA navigation fallback — production builds boot fully offline (content already comes from IndexedDB). Installable: web app manifest + icon. Applies to `pnpm build` output only; the dev server does not emit the service worker.
- **Local-first read path (ROADMAP Phase 2):** the reader now reads from the device. Import writes the parsed book (sections + blocks + images + cover) to IndexedDB (Dexie) before ingest — readable immediately; chapter turns, TOC, and images are served from IndexedDB with no network I/O. Books imported on another device fall back to Convex and cache-fill locally (section metadata, blocks, and images) so subsequent reads are local. Deleting a book purges the local copy. `RequireAuth` gains offline grace: a previously signed-in device renders local content when offline. Progress/bookmarks still sync via Convex (durable local progress lands with the Phase 4 sync layer).
- **Static SPA (ROADMAP Phase 1):** removed TanStack Start and the nitro SSR server; the app is now a plain Vite + TanStack Router single-page app (`index.html` + `src/main.tsx` entry, route code-splitting via `@tanstack/router-plugin`). `vite build` emits a fully static `dist/` deployable to any static host. Auth is unaffected (Better Auth is served by Convex HTTP actions, not the removed Node server). Drops the `nitro` nightly pin and `@tanstack/react-start`/`react-router-ssr-query` dependencies.

## 0.1.0 - 2026-01-26

- New TanStack Start + Convex stack with Better Auth (email/password).
- EPUB import pipeline with parser service integration and Convex storage.
- Library view with covers, sorting, search, and protected actions.
- Reader experience with TOC, next/prev, bookmarks, and preferences.
- Progress tracking and resume support.
- Added browser tests (Vitest Browser Mode) and Playwright smoke tests.

  ## 0.1.1 - 2026-01-26
  - Remove unused template files/assets (benchmarks, TanStack scaffolding, sample logos).
  - Clean up .gitignore.

## 0.1.2 - 2026-07-03
- Upgrade all web dependencies to latest; npm audit vulnerabilities cleared (44 → 0), including better-auth 1.6 + Convex adapter 0.12, Convex 1.42, Vite 8, TanStack Start/Router 1.168, and TypeScript 6.
- Upgrade the Go parser: toolchain go 1.25.11 (stdlib CVE fixes), Spine 0.3.1, and golang.org/x/* to latest; govulncheck clean (19 → 0).
- Minor fixes surfaced by the upgrade: remove deprecated tsconfig `baseUrl`, add the missing default export on the library route, and match Convex functions by name in the Library test mock.

## 0.2.0 - 2026-07-04
- Parse EPUBs in the browser: replaced the Go parser microservice with **Spine-TS**, a TypeScript port of Spine (`web/src/spine`). Removes the third deployable service and its hosting/reachability requirements. Validated exact/near-exact against Go Spine on a corpus of real books.
- Scalable import: in-browser parse + batched, blocks-only ingest via storage upload URLs — no book-sized payload passes through a Convex function, fixing the 64 MB out-of-memory failure on large webnovels (thousand-chapter books now import and read).
- Blocks-only data model: sections store per-section structured blocks (no duplicated plain text); reading progress is block-anchored (`lastBlockIndex`/`lastBlockOffset`); bookmarks use `blockIndex`.
- Removed the Go `/parser` service, `PARSER_URL`, and related Makefile/README wiring.
- **Breaking:** the data model changed; existing dev data must be reset (`make convex-reset CONFIRM=RESET`).

## 0.3.0 - 2026-07-04
- Extracted the EPUB parser into its own package, **[`@abfcode/spine`](https://www.npmjs.com/package/@abfcode/spine)** (published to npm); Librium now consumes it as a normal dependency.
- Parallelized the import ingest (concurrent batches) and virtualized the chapter list (TOC) — large books import and browse smoothly.
- Fixed book deletion to run in bounded batches so thousand-chapter books stay under Convex's per-mutation read limit.
- Self-hosted fonts (Fraunces + IBM Plex Sans via `@fontsource`), removing the third-party Google Fonts request (blocked by Firefox tracking protection / adblockers).
